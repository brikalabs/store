{
  "name": "@brika/env",
  "version": "0.1.0",
  "private": true,
  "description": "Typed, validated, cached Worker env accessors (secrets + vars) built on Zod",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "!src/**/*.test.ts"
  ],
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
