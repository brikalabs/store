{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM"],
    // Transitive: `@brika/store-db` uses `D1Database` (workers-types) and `@brika/di` uses
    // `node:async_hooks` (via the `bun` types superset).
    "types": ["@cloudflare/workers-types", "bun"]
  },
  "include": ["src"],
  "exclude": ["src/**/*.test.ts"]
}
