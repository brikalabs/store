export { type Db, getDb } from "./client";
export * as schema from "./schema";
export {
  regAudit,
  regDeviceAuth,
  regDistTags,
  regPackages,
  regScopes,
  regTokens,
  regVersions,
} from "./schema";
