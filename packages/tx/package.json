{
  "name": "@brika/tx",
  "version": "0.1.0",
  "private": true,
  "description": "Compensating-transaction engine (saga) for side effects spanning systems with no shared transaction (R2 + D1): rollback/commit/completion hooks, Spring-style propagation, a @transactional decorator, and transactional wrappers for storage + the ORM. Concurrency-safe via AsyncLocalStorage.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/brikalabs/store.git",
    "directory": "packages/tx"
  },
  "license": "MIT",
  "author": "Brika Labs",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "!src/**/*.test.ts"
  ],
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "hono": "^4.12.25",
    "typescript": "^6.0.3"
  }
}
