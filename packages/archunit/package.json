{
  "name": "@brika/archunit",
  "version": "0.1.0",
  "private": true,
  "description": "A tiny ArchUnit-style architecture-rule engine: declare layering rules fluently by package/folder glob (`mayNotImport`), run them as a lint CLI or as test cases. Import-aware (comments stripped), framework-agnostic.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/brikalabs/store.git",
    "directory": "packages/archunit"
  },
  "license": "MIT",
  "author": "Brika Labs",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "!src/**/*.test.ts"
  ],
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^6.0.3"
  }
}
